# Cleaning Guide Backend

이 프로젝트는 "집 청소 가이드 앱"의 백엔드 서버로, 장소별 청소 루틴과 필요한 준비물을 제공하는 조회 전용(Read-only) REST API를 제공합니다. 프론트엔드는 미리 데이터베이스에 저장된 장소, 준비물, 루틴 데이터를 단순 조회하여 사용자에게 보여주는 구조로 설계되었습니다.

---

## 📌 주요 기능 (Features)

이 백엔드 서버는 **CRUD 중 조회(Read)** 기능만 제공합니다.

### ✔ Place (장소)

* 모든 장소 조회
* 장소 ID로 단일 장소 조회

### ✔ Supply (준비물)

* 특정 장소의 준비물 목록 조회
* 특정 준비물 상세 조회

### ✔ Routine (청소 루틴)

* 특정 장소의 전체 루틴 단계 조회 (orderIndex 기준 정렬)
* 특정 루틴 단계 상세 조회

---

## 📌 기술 스택 (Tech Stack)

* **Java 17**
* **Spring Boot**
* **Spring Web**
* **Spring Data JPA**
* **MySQL**
* **Swagger (API 문서 자동화)**

---

## 📌 프로젝트 구조 (Project Structure)

```
backend/src/main/java/com/backend
 ├── controller      # API 엔드포인트 제공
 ├── dto             # 클라이언트에 전달되는 데이터 구조
 ├── entity          # JPA 엔티티 (DB 테이블 매핑)
 ├── mapper          # Entity <-> DTO 변환
 ├── repository      # JPA Repository (DB 조회)
 ├── config          # SwaggerConfig, CorsConfig
 └── BackendApplication.java
```

---

## 📌 API 엔드포인트 요약

### 🔹 Place API

| Method | URL                    | 설명       |
| ------ | ---------------------- | -------- |
| GET    | `/api/place`           | 전체 장소 조회 |
| GET    | `/api/place/{placeId}` | 장소 상세 조회 |

### 🔹 Supply API

| Method | URL                             | 설명               |
| ------ | ------------------------------- | ---------------- |
| GET    | `/api/supply/{placeId}`         | 특정 장소의 준비물 목록 조회 |
| GET    | `/api/supply/detail/{supplyId}` | 특정 준비물 상세 조회     |

### 🔹 Routine API

| Method | URL                               | 설명              |
| ------ | --------------------------------- | --------------- |
| GET    | `/api/routine/{placeId}`          | 특정 장소의 전체 루틴 조회 |
| GET    | `/api/routine/detail/{routineId}` | 루틴 단계 상세 조회     |

---

## 📌 실행 방법 (How to Run)

# Cleaning Guide Backend

이 프로젝트는 "집 청소 가이드 앱"의 백엔드 서버로, 장소별 청소 루틴과 필요한 준비물을 제공하는 **조회 전용(Read-only) REST API**를 제공합니다. 모든 데이터는 운영자가 데이터베이스에 사전에 입력해두며, 클라이언트(iOS)는 이 API를 통해 데이터를 조회하여 사용자에게 노출합니다.

---

# 📌 주요 기능 (Features)

이 백엔드 서버는 **정적 데이터 기반 조회 기능**만 제공합니다.

### ✔ Place (장소)

* 전체 장소 조회
* ID로 단일 장소 조회

### ✔ Supply (준비물)

* 특정 장소의 준비물 목록 조회
* 준비물 ID로 단일 준비물 조회

### ✔ Routine (청소 루틴)

* 특정 장소의 전체 청소 루틴 조회 (orderIndex 기준 정렬)
* 루틴 ID로 단일 루틴 단계 조회

---

# 📌 기술 스택 (Tech Stack)

* **Java 17**
* **Spring Boot**
* **Spring Web**
* **Spring Data JPA**
* **H2 / MySQL** (환경에 따라 변경 가능)
* **Swagger (API 문서 자동 생성)**
* **Lombok**

---

# 📌 프로젝트 구조 (Project Structure)

```
backend/src/main/java/com/backend
 ├── controller      # API 엔드포인트 제공
 ├── dto             # 클라이언트에 전달되는 데이터 구조
 ├── entity          # JPA 엔티티 (DB 테이블 매핑)
 ├── mapper          # Entity <-> DTO 변환
 ├── repository      # DB 조회용 JPA Repository
 ├── config          # SwaggerConfig, CorsConfig
 └── BackendApplication.java
```

---

# 📌 API 엔드포인트 요약

## 🔹 Place API

| Method | URL                    | 설명       |
| ------ | ---------------------- | -------- |
| GET    | `/api/place`           | 전체 장소 조회 |
| GET    | `/api/place/{placeId}` | 장소 상세 조회 |

## 🔹 Supply API

| Method | URL                             | 설명               |
| ------ | ------------------------------- | ---------------- |
| GET    | `/api/supply/{placeId}`         | 특정 장소의 준비물 목록 조회 |
| GET    | `/api/supply/detail/{supplyId}` | 준비물 상세 조회        |

## 🔹 Routine API

| Method | URL                               | 설명              |
| ------ | --------------------------------- | --------------- |
| GET    | `/api/routine/{placeId}`          | 특정 장소의 전체 루틴 조회 |
| GET    | `/api/routine/detail/{routineId}` | 단일 루틴 단계 조회     |

---

# 📌 실행 방법 (iOS 협업용)

이 프로젝트는 iOS 팀과 협업하여 제작되므로, **백엔드 & iOS 연동 환경**을 중심으로 실행 방법을 정리했습니다.

## 1. 백엔드 서버 실행

백엔드 개발자는 아래 명령으로 서버를 실행합니다.

### ✔ IntelliJ

* `BackendApplication.java` 실행

서버 기본 URL:

```
http://localhost:8080
```

---

## 2. iOS 팀이 사용할 API Base URL

### ✔ iOS 시뮬레이터

```
http://127.0.0.1:8080
```

### ✔ iOS 실기기 테스트

실제 기기가 백엔드 개발자의 로컬 서버에 접근해야 하므로, 개발자의 로컬 IP를 공유해야 합니다.

예시:

```
http://192.168.X.X:8080
```

> 동일 Wi-Fi 환경에서만 접속 가능함

---

## 3. API 문서(Swagger) 확인

서버 실행 후 아래 주소 접속:

```
http://localhost:8080/swagger-ui/index.html
```

여기에서 iOS 개발자가 필요한 모든 API 구조, 파라미터, 응답 예시를 확인할 수 있습니다.

---

---

## 📌 개발 의도 (Purpose)

이 프로젝트는 사용자에게 집 청소를 쉽고 체계적으로 제공하기 위한 **정적 데이터 기반 조회 시스템**입니다.

* 데이터는 운영자가 DB에 미리 입력
* 사용자 앱에서는 장소 → 준비물 → 청소 루틴을 직관적으로 탐색
* 복잡한 CRUD가 필요 없는 가벼운 구조

---

